/**
 * Test sagas
 */

// import { take, call, put, select } from 'redux-saga/effects';
// import previewSaga from '../saga';

// const generator = previewSaga();

describe('previewSaga Saga', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
