/*
 * RightSideDrawer Messages
 *
 * This contains all the text for the RightSideDrawer component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RightSideDrawer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RightSideDrawer component!',
  },
});
